# xiaozhuan-seal 接入调研（PRD F3 文字层 · 汉字篆体转化 + 印文放置）

> 调研对象：https://github.com/xsy1126/xiaozhuan-seal（clone 至 /tmp/xiaozhuan-seal，87 文件 8.2MB）。
> 定位：接入本项目 /api/design-render 的**文字层**（质感层/文字层分离架构的后半段，PRD F3）。
> 用户原话：「篆刻石是载体，印是灵魂」——文字层即灵魂层。
> 本文件为只读调研产物，未改动任何源代码。日期：2026-09-06。

---

## 用户拍板记录（2026-09-06，推翻调研部分结论）

1. **峄山碑字体入栈**：demo 阶段 3754 字覆盖足够，峄山碑直接作为产品字体栈（调研曾建议不入栈）。合规注记：授权「免费商用+禁修改」——与崇羲同模式「只渲染不分发」，woff2 分片放 app/fonts/（构建资产，非 public/ 可下载路径），产出物标注字体来源「峄山碑篆体（字传 · 免费商用）」。
2. **字序**：按 PRD 05 正确字序实现（先上后下先右后左，四字=右上→右下→左上→左下）——demo 的现代左起映射已改（lib/seal-face/layout.ts assignCells）；三字排法为暂定式待文化评审。
3. **图片导出后置**：嵌入完成后再加，本轮不做。

**实施形态（方案 A 快速版 · 已实施）**：客户端 React 组件版（SealFaceProof）——峄山碑仅 woff2 unicode-range 分片形态，服务端解析需 wawoff2+opentype.js 双依赖，浏览器对 cn-font-split 产物原生按需加载；排布/错落/斑驳三件写成纯 TS 模块（lib/seal-face/layout.ts）保证后续可迁服务端（D 桶路径不封死）。

## 0. 一页结论

- **demo 能力**：纯浏览器端（DOM+CSS+SVG）的四字小篆印章工具——字体渲染 + 2×2 固定格排布 + 朱白配色切换 + seed 随机错落 + 印泥斑驳 + 按字形真实边界的紧凑铺满；**服务端不可直接复用**（依赖 document.fonts/canvas 等 browser API）。
- **推荐方案**：**方案 B —— 提取三个可移植件（紧凑排布算法思想 / seed 错落参数模型 / 斑驳磨损 SVG 生成器）+ 崇羲字体自建取形与章法引擎**。字形源**不用** demo 的峄山碑篆体（字数少 1/3、禁修改、社区报字形错误、无明确署名条款——四项均劣于既定崇羲 CC BY-ND 3.0 基准）。
- **最大风险**：demo 的字序是**现代左起序，违反 PRD 05 传统章法硬约束**（先右后左）——排布映射一行都不能照抄；其次是白文「镜像」的输出物语义（印面视图 vs 印蜕视图）需在 M6 规格中先钉死。

---

## 1. Demo 摸底

### 1.1 来源链与许可

| 层 | 对象 | 许可 |
|---|---|---|
| 本体 | xsy1126/xiaozhuan-seal | MIT（代码），fork 自下者 |
| 上游 | JackerKun/XiaoZhuan（2026） | MIT（代码，README 保留署名） |
| 字体 | **峄山碑篆体**（`@chinese-fonts/ysbzt@3.0.0`，55 个 woff2 分片 + unicode-range 子集，1.7MB） | npm 包 MIT **仅覆盖 cn-font-split 打包代码**；**字体本体为「作者声明授权」**：免费商用（含嵌入软件/App/商标）、**禁止修改与衍生**、禁止转售字库（猫啃网取得分发授权，核验于 maoken.com/freefonts/21952.html） |
| 字典数据 | `js/data.js`（2503 字 + 说文解字引用 + 拼音 + 19 主题分类） | 随代码 MIT |

字体背景：咸阳职业技术学院学生科创项目《峄山石刻字库开发》（字传发布）；以李斯《峄山刻石》为蓝本电脑路径临摹复刻，碑外字以《说文解字》字型部首推衍。**实测覆盖 3754 字**（GB2312 3754/6763、常用字表 3374/3500——README 的 2503 是字典功能收录数，非字体数）。

### 1.2 功能边界（four.html 印章工具，核心 js/four.js 51 行 + js/stamp.js 70 行）

| 能力 | 实现方式 | 细节 |
|---|---|---|
| 汉字→小篆 | CSS `font-family:"峄山碑篆体"` 直接渲染 | 浏览器字体栈，无字形运算 |
| 缺字检测 | `document.fonts` 的 `unicodeRange` 逐字检查 | 命中即提示「当前小篆字体未收录：X。请换成其他汉字」——**如实告知 ✓**（与 PRD 反例 #10 精神一致） |
| 印文排布 | 仅 **2×2 四字**固定格；映射 `chars[[0,2,1,3][i]]` | 顺序 = 第 1 字左上、第 2 字左下、第 3 字右上、第 4 字右下——**现代左起序**（例「天下太平」显示为左列「天下」右列「太平」，横读通顺但**钤印读序错误**） |
| 朱白文 | CSS class 切换配色：朱文=红字纸底红框；白文(`.white-style`)=**红底纸字** | 钤印（印蜕）视角的正确呈现；**无字形镜像、无笔画几何反相**（那是刻制/印面视图的概念） |
| 章法错落 | LCG seed 伪随机 → 每字 CSS 变量 `--dx/--dy/--sx/--sy`（平移±0.6%、缩放±0.9%） | 「帮我 shuffle」= seed+1，**可复现随机**；自由度滑杆三档（规整/适中/自在） |
| 印泥斑驳 | 纯 SVG ellipse 生成器：210 个随机 fleck + 44 个浅边缘磨损（保留可读边界） | **服务端可移植**（无浏览器依赖） |
| 刀刻质感 | SVG filter `feTurbulence + feDisplacementMap`（carved 滤镜，位移 2.6） | 同上可移植 |
| 紧凑铺满 | **fitStampGlyphs（本 demo 最有价值算法）**：`canvas.measureText` 的 `actualBoundingBoxLeft/Right/Ascent/Descent` 取**字形真实墨迹边界**（非字体 bearings）→ 每字替换为内联 SVG，viewBox 裁到墨迹边界 + `preserveAspectRatio:none` 非均匀拉伸 | 「根据实际字形边界压缩留白，四字紧凑铺满印面」——汉印「均分印面」的现代等价实现 |
| 输出形态 | DOM + 内联 SVG + CSS（**非图片导出**） | 无 PNG/SVG 下载功能 |
| 界格 | 淡红细线（`border` 半透明） | 有，但仅田字一种 |

### 1.3 技术栈与可提取性

- **零依赖纯静态**（HTML/CSS/vanilla JS，唯一 npm 依赖是字体包）；无 TS、无构建。
- **强浏览器耦合**：`document.fonts.load/ready`、`canvas.measureText`、DOM 渲染——**不能进 Next.js API 路由**（serverless 无 headless browser 预算）；嵌入主站只能作客户端组件/iframe。
- 代码质量：小而清晰（核心 121 行 JS），注释诚实（README 明言「基于字体渲染，不进行古文字字源考证」）。
- 顺带资产：`js/data.js` 2503 字说文解字/分类数据（M8 印章文化元素库的候选补充源，MIT）。

---

## 2. PRD F3 需求对照表

| # | F3 要求（PRD 11.2-F3 / 8.1） | demo 能力 | 判定 |
|---|---|---|---|
| 1 | fonttools（或等价）取形 | 无——CSS 字体渲染 | ✗ 需自建（Node 端 opentype.js 等价） |
| 2 | 规则引擎排布：字序右上起（先右后左）、四字 6 种排法枚举、界格体制 | 仅 2×2 一格 + **字序违规（先左后右）** | ✗ 映射不可抄；「字数→网格枚举」需按 05 报告自建 |
| 3 | 白文字形镜像 + 笔画反相 | CSS 配色反转（红底纸字） | △ 印蜕视角等价可用；印面视图的镜像/反相需自建 |
| 4 | 缺字如实告知（禁造字） | unicodeRange 检查 + 明确提示 | ✓ 机制到位；服务端改为字体 cmap 查询 |
| 5 | 禁止模型造字 | 字体驱动，天然零造字 | ✓ 架构同源（确定性字体引擎铁律） |
| 6 | 繁简映射（崇羲以《说文》为准） | 峄山碑为简码位直映（「简转繁的字体」，评论区实证） | ✗ 与崇羲字形体系不一致，不可沿用其假设 |
| 7 | 印面比例适配（均分印面/疏密） | **fitStampGlyphs 真实边界紧凑算法** | ✓✓ 核心可移植资产（思想 + 参数可参考） |
| 8 | 叠入质感层的合成方式 | 无合成概念（独立展示页） | ✗ 需自建（sharp / SVG 叠加管线） |

小结：8 项中 demo 直接可用 1.5 项（缺字告知机制 + 印蜕视角朱白配色），思路级可移植 2 项（紧凑算法、随机错落），不可用 4.5 项。

---

## 3. 接入方案评估

### 方案 A：直接嵌入（demo 模块进文字层）

- 做法：four.html/stamp.js 重写为 React 客户端组件（或 iframe），嵌在 /design-render 页客户端出印面。
- 工作量：2-3 天（组件化 + 样式迁移）。
- **否决理由**：① 与三站架构冲突——F3 是管线层（`/api/design-render` 后叠加合成，D 桶要「印面区域与渲染稿一致、叠字位置无漂移」的确定性验收），客户端 DOM 渲染不可复现、不可进 CI；② 字体源不合规优势全失（峄山碑 3754 字 + 禁修改 + kern 损坏 + 简转繁直映）；③ 字序违规；④ 输出非图片，无法与质感层合成。

### 方案 B：提取排布/质感逻辑 + 崇羲自建取形（**推荐**）

- **提取三件**（均为零浏览器依赖或可等价替换）：
  1. **fitStampGlyphs 紧凑算法**：思想移植——Node 端用 opentype.js（或 fonttools 离线预取）读 glyph 的 `getBoundingBox()`（等价 actualBoundingBox），按墨迹边界而非 em-box 排格，实现「均分印面、任疏任密」的汉印基线。
  2. **seed 错落参数模型**：LCG + dx/dy/sx/sy 参数范围（±0.6%/±0.9%）与三档自由度——直接抄参数与结构（几十行），换 PRD「章法软约束评分」驱动幅度。
  3. **斑驳/磨损 SVG 生成器 + carved 滤镜**：`stamp.js` 的 fleck 循环与 feTurbulence 配置可原样移植到服务端 SVG 输出。
- **自建三件**：① 崇羲字体取形（cmap 查字 → glyph path + bbox；缺字 → 如实告知+替代建议）；② 05 章法规则引擎（字序先右后左、1-4 字网格枚举、界格白名单、朱白互换条件）；③ 质感层合成（sharp 或 SVG overlay，把印面层叠到 /api/design-render 的素坯图上）。
- 与现有架构衔接：
  - `seal-order.ts`：`seal_text / text_count / seal_style / seal_form` 已是文字层全部入参——**天然对齐，零改动**。
  - `seal-prompt.ts`：NO TEXT 铁律保证质感层无字，文字层独立后叠加——**架构验证了分离决策的正确性**（demo 恰好反过来：只有文字层没有质感层）。
  - 白词语义（需 M6 钉死）：**印蜕视图**（钤印效果，正字红底白字）不镜像——demo 的配色反转可参考；**印面视图**（石头底部刻面）需镜像——自建 transform。
- 工作量：**4-6 人日**（取形 1-1.5、章法引擎 1-1.5、合成与朱白 1-1.5、斑驳质感移植 0.5、联调与 C 桶校验 1）。
- 风险：崇羲字体文件获取与 opentype.js 兼容性（半天预跑项，PRD R4 已列）；繁简映射需文化评审。

### 方案 C：仅参考思路自研

- B 已把「可提取的」与「必须自建的」划清——C 等于放弃现成资产（尤其斑驳生成器与紧凑算法参数），无增益。**不取**。

---

## 4. 实施步骤（方案 B，含 M4 时序）

```
① 崇羲预跑（半天，先于一切）：
   获取崇羲字体 → opentype.js 解析 cmap/glyph bbox → 验证「劉雨茜」类姓名字覆盖
② 文字层模块 lib/seal-face/（1.5-2 天）：
   glyph.ts（取形+bbox+缺字查询）→ layout.ts（05 章法引擎：字序/枚举/界格
   + fitStampGlyphs 式墨迹紧凑）→ style.ts（朱白：印蜕配色 / 印面镜像反相）
③ 质感合成层（1 天）：
   texture-composite.ts——印面 SVG 层（含移植的斑驳 fleck 生成器 + carved
   滤镜参数）→ sharp/SVG 叠加到 /api/design-render 质感层产出的印面区域
④ 管线接入（0.5-1 天）：
   /api/design-render 第二阶段：质感层生成（现状不动）→ seal_text 非空时
   叠加文字层 → 输出组图；SealOrder 零改动
⑤ C 桶校验（0.5-1 天）：
   逐字与崇羲 cmap 比对的零错误测试进 CI（PRD 16-C blocking 门禁）
```

时序原则：**文字层永远在质感层生成之后叠加**（PRD F4 五步管线的第④步），质感层 prompt 的 NO TEXT RULE 不变。

---

## 5. 风险清单

| # | 风险 | 等级 | 处置 |
|---|---|---|---|
| R1 | **字序违规**：demo 先左后右，PRD 05 硬约束先右后左 | 高 | 排布映射一律自建；demo 映射不抄一行 |
| R2 | **峄山碑字体四劣**：3754 字（崇羲 1/3 覆盖）/ 禁修改 / 社区实证字形错误（kern 双组自相矛盾、「汉」疑误）/ 简码直映非《说文》体系 | 高 | 字形源**只用崇羲**（PRD 铁律不变）；峄山碑仅作渲染参考，不入产品字体栈、不分发其文件 |
| R3 | 崇羲 CC BY-ND 3.0 合规：禁改字型、需署名、只渲染不分发 | 中 | 产出物（效果图页脚/水印）加字体署名；服务端字体文件不进 public/（进 tracing 仅限 API bundle） |
| R4 | 白文「镜像」语义未钉死（印面 vs 印蜕两视图） | 中 | M6 成品图规格先行定义；印蜕=配色、印面=镜像+反相，两个都要 |
| R5 | 繁简映射规则（PRD 14 待文化评审） | 中 | 映射表独立模块，评审后可改；峄山碑的简码直映假设不可沿用 |
| R6 | opentype.js 对崇羲字体的兼容性未知 | 中 | 半天预跑（R4 of PRD 既有安排）；fallback：fonttools 离线预取字形 JSON |
| R7 | demo 上游 MIT 署名义务（提取斑驳生成器等代码段时） | 低 | 提取件注释注明 `// ported from JackerKun/XiaoZhuan (MIT)` |

---

## 6. 附：与素材库的呼应

`public/seal-references/seal-faces/`（30 张实物印面/印蜕：白文 14 / 朱文 12 / 朱白相间 4）正是文字层的 **D 桶评测基准**——自建管线的输出与这批真实印蜕做视觉比对（人工评分 ≥3/5，PRD 16-D），demo 的紧凑度与斑驳自然度可作为调参的直观参照。
